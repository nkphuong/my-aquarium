# Design Brief

  ## Overall Aesthetic
  - Style: I want it bright, modern, playful and cute
  - Mood: calm and friendly
  - Inspiration: `login_ref.png` for the authentication page, `dashboard_ref.png` for the dashboard page 

  ## Colors
  - Primary: pick the color in the `dashboard_ref.png`
  - Secondary: pick the color in the `dashboard_ref.png`
  - Accent: pick the color in the `dashboard_ref.png`
  - Background style: pick the color in the `dashboard_ref.png`

  ## Typography
  - Headings: pick the font in the `dashboard_ref.png`
  - Body: pick the font in the `dashboard_ref.png`

  ## Components Style
  - Buttons: pick the style in the `dashboard_ref.png`
  - Cards: pick the style in the `dashboard_ref.png`
  - Inputs: pick the style in the `dashboard_ref.png`

  ## Layout Preferences
  - Spacing: pick the style in the `dashboard_ref.png`
  - Navigation: pick the style in the `dashboard_ref.png`
